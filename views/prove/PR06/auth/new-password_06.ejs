<%- include('../includes/head.ejs') %>

</head>

<body>
    <%- include('../includes/nav2.ejs') %>

    <main>

        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-4">
                    <% if (errorMessage) { %> 
                        <div class="mb-3"><%= errorMessage %> </div>
                    <% } %>
                    <form action="/reset" method="POST">

                        <div class="form-group mb-3">
                            <input type="password" class="form-control" name="password" id="password" placeholder="Password">
                        </div>

                        <div class="form-group mb-3">
                            <input type="password" class="form-control" name="conf-password" id="conf-password" placeholder="Confirm Password">
                        </div>

                        <button class="btn btn-outline-primary mt-2" type="submit">Reset Password</button>
                        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                        <input type="hidden" name="userId" value="<%= userId %>">
                    </form>

                </div>
            </div>    
        </div>


    </main>
<%- include('../includes/end.ejs') %>